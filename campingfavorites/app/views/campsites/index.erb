<% if flash.has?(:message) %>
  <%= flash[:message] %>
<% end %>

<% @campsites.each do |c|%>
<!-- add delete edit/buttons if created_by == current_user -->
	<p>
	<a href='/campsites/<%= c.id %>'><%= c.name %></a>
	</p>
<%end%>
